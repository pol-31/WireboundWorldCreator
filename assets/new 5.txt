+ label field for each: kTerrainEditScaleXText
+ error line below
+ SliderH2 - bigger handle(icon), modify arae (remove embedded handle/icons)
------------------------------------------------------------------
------------------------------------------------------------------
------------------------------------------------------------------

07.09
do we need WindowQueue both for global data and IUiEdit modes?
window dissappearing by fire

How to represent terrain in 3d? correct:layers, BUT how to be with mountain climbing?
okay, NO abcose angles - ONLY heightmap, but layered on Y.
SO: ArbitraryGraph.h


TODO:
BUG
NEW
- sliders scale
---> terrain layers (each store all noises settings) Add/Remove updated basic cur_tile_.hmap_
---> group & align (no pain preferred)
- description relevance?
PURGATORY (useless / temp imposs)




//TODO:
// 1. line: O-O-O-O-O
// - each one has 1 parent (let't take most left)
// - each one has {float_scale, glm::vec2 offset}
// this way NO INTERNAL TRANSFORM (via Ctrl button)
// TODO: how to get it: parse ../assets/VboTransform.txt...
//
// OR... let's skip it for now.

----------------------------------------------
----------------------------------------------
----------------------------------------------
NEW ISSUES:
- UiConfirmation (caution, file, etc...) shadow + text sprites (FREE ALL kSpareText)
- UiSliderV2 for noise config window (take sprites from in UiSlots)
- TextRenderer::prerender_text_slot
- Noise config (separate windows): hmap sprites
- Bake config: texture's names (Texture32F& + UiDynamicSprite)
- keyboard assign window (stones in position raisen not)
- UiSlotsTerrain - btn RESET - to set default config
- desaturate color indicator
- noise layer sprite (left bottom corner) + рамочка
- UiTerrainMode::slots remove edit/select, remove vertex/edge/face
- show pressed keys (top left corner: translate/scale/rotate x/y/z, etc...)
- UiTopWindow --> SHADOW SPRITE
- remove sprite_keyboads (not going to reassign keys)
- remove kSpareText (yes, again)



TODO(kept unresolved):
- FlattenTerrain.comp shader
- render/show hmap after processing (transformation, tiling, invert, flatten/cut >90deg)
# apply transform to tex_coords?
# flatten <90deg angles

# Add to UiText ctors:
int area_length,
ing char_limit (for __std::vector area_char_limit)


TODO:
0. text input limit
1. terrain layer transform
2. adjust noise gen params (broken because of unresolved terrain layer transform)
3. slot color indicator & name (UiSlots gets from UiEditTerrain)
4. selection orientation fix
5. UiFile, Parse / Serialize (load/save -> window, etc...)
6. console? but for what
